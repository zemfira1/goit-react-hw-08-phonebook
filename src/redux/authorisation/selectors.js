export const selectIsLoggedIn = state => state.authorisation.isLoggedIn;
export const selectUser = state => state.authorisation.user;
export const selectIsRefreshing = state => state.authorisation.isRefreshing;
