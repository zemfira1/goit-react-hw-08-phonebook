import styled from '@emotion/styled';
import { theme } from 'styles';

export const Anything = styled.div`
  font-size: ${theme.fontSizes.xl};
  width: ${theme.spacing(300)};
  margin-inline: auto;
  margin-top: ${theme.spacing(7)};
  text-align: center;
`;
